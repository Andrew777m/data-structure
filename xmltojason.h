#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <stack>
using namespace std;
void xml_to_json(vector<string>& xml, vector<string>& json, unsigned int& size);
void PARSE(string file_data, vector<string>& XML_Vector, unsigned int& XML_Size);
